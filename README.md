# Awards

Rodar o server:
npm run devStart

Container docker:
docker-compose up -d

Acessar terminal docker:
docker exec -it awards_mysql mysql -u root -p
senha: root 
use awards_database;

Rodar o frontend:
npm run dev